import numpy as np
number = np.random.randint(1,101)               # Загадали число
count=0                                         # Ввели счетчик попыток 
if number == 1:                                 # Проверяем, что заданное число не равно крайним значениям
    print("Вы угадали число {number} за 1 попытку")
elif number == 100:
    print("Вы угадали число {number} за  попытки")
else:
    x=1                                         # Присваиваем переменным крайние значения множества
    y=100
    while True:                                 # Запускаем цикл считаем попытки начиеная с третьей, т.к. первые две мы проверяли 
        count +=3
        for i in range(x,y+1):
            predict = int((x+y)/2)              # Выводим среднее значение множества. В результате у нас множество делится 
#на 2 подмножечства и мы остаемся искать значение в том подмножестве, где находится искомое число. Так до получения результата
            if number==predict:
                break
            elif number > predict:              
                x = predict+1
            else:
                y = predict-1
    print("Вы угадали число {number} за {count} попыток.")
    
